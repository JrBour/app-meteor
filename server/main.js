import { Meteor } from 'meteor/meteor';
import '../imports/api/StudentCollection.js';
import '../imports/api/ClasseCollection.js';
import '../imports/api/CourseCollection.js';

Meteor.startup(() => {
  // code to run on server at startup
});
